info:
  name: my-invites
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5000/api/me/invites
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

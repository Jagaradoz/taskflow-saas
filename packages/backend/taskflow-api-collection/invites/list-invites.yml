info:
  name: list-invites
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5000/api/orgs/:orgId/invites
  params:
    - name: orgId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

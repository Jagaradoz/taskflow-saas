info:
  name: create-org
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/orgs
  body:
    type: json
    data: |-
      {
        "name": "My Organization"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

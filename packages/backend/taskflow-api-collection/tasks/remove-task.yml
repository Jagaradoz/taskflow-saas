info:
  name: remove-task
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:5000/api/tasks/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: list-tasks
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

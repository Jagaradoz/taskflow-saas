info:
  name: update-member
  type: http
  seq: 2

http:
  method: PATCH
  url: http://localhost:5000/api/members/:id
  params:
    - name: id
      value: PASTE_ID_FROM_list-members_RESPONSE
      type: path
  body:
    type: json
    data: |-
      {
        "role": "member"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

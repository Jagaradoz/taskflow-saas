info:
  name: update-member
  type: http
  seq: 2

http:
  method: PATCH
  url: http://localhost:5000/api/members/:id
  auth: inherit
  params:
    path:
      id: ""
  body:
    type: json
    json: |
      {
        "role": "admin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

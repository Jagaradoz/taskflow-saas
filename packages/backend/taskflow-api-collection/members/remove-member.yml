info:
  name: remove-member
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:5000/api/members/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: remove-member
  type: http
  seq: 2

http:
  method: DELETE
  url: http://localhost:5000/api/members/:id
  params:
    - name: id
      value: PASTE_MEMBERSHIP_ID_FROM_list-members_RESPONSE
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

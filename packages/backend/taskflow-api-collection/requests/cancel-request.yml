info:
  name: cancel-request
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:5000/api/me/requests/:requestId
  params:
    - name: requestId
      value: PASTE_ID_FROM_create-request_RESPONSE
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
